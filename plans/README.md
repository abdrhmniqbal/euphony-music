# Implementation Plans

Generated by improve skill on 2026-06-14. Execute in order below unless dependencies say otherwise. Each executor: read plan fully before starting, honor STOP conditions, run verification gates, and update status row when done.

## Recon Summary

- App: Startune Music, offline-first local music player.
- Stack: Expo SDK 56, React Native 0.85, React 19.2, Expo Router, TypeScript, SQLite/Drizzle, Zustand, TanStack Query, HeroUI Native/Uniwind, React Native Track Player fork.
- Package manager target: pnpm. Current repo has `pnpm-lock.yaml` and agent docs already recommend pnpm, but README/build docs/CI/system map still reference Bun.
- Verification commands from repo: `pnpm install`, `pnpm run start`, `pnpm run android`, `pnpm run ios`, `pnpm run web`, `pnpm run lint`, `pnpm run format`, `pnpm run knip`. Expo-specific plan also uses `pnpm exec expo install --check`, `pnpm exec expo install --fix`, and `pnpm exec expo doctor`.
- Test coverage shape: no explicit `test` script found in `package.json`; verification currently depends on lint, knip, Expo doctor, and device/build smoke.
- Deployment/release target: Android APK release workflow in `.github/workflows/release-apk.yml`; EAS config present in `eas.json`.

## Findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---|---|---|---|---|---|
| 1 | Bun-to-pnpm migration incomplete while dependency upgrade is planned | migration / dx | CI and onboarding can install with wrong package manager; `bun.lock` cache key likely stale/missing; root Bun dependency bloats and pollutes pnpm lockfile | M | MED: dependency/native upgrades can break Expo/RN peer ranges | `package.json:34`, `pnpm-lock.yaml:53-55`, `pnpm-workspace.yaml:5`, `.github/workflows/release-apk.yml:44-68`, `README.md:52-68`, `SYSTEM_MAP.md:11`, `docs/build-guide.md:12-29` |

## Direction Findings

- Dependency upgrade should be handled as one controlled migration, not mixed into feature work. Evidence: app has many native dependencies and release workflow writes native Android version files during CI (`.github/workflows/release-apk.yml:115-175`), while Expo config enables new architecture and React Compiler (`app.json:3`, `app.json:17-18`). Trade-off: slower than broad `pnpm update`, but safer for Expo native compatibility.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Upgrade Expo and dependencies under pnpm | P1 | M | — | TODO |
| 002 | Rewrite core engine under Startune UI | P1 | L | 001 if still TODO | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- `001` stands alone. It should land before feature work that touches native modules, release workflow, or dependency-sensitive UI/runtime code.
- `002` should run after `001` if the package-manager/dependency migration is still pending, because implementation port needs one stable verification command set.

## Findings Considered And Rejected

- Full-codebase correctness/security/performance audit: out of scope for user request, which specifically asked for dependency upgrade planning and Bun removal.
- Adding a test runner baseline: valuable long-term, but not prerequisite for a package-manager/Expo upgrade plan because repo currently has no `test` script and upgrade verification can use Expo doctor, lint, knip, and Android smoke.
